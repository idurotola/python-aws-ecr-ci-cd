version: 2
jobs:
  build:
    docker:
      - image: tiangolo/uwsgi-nginx-flask:python2.7
    working_directory: /tmp/lab
    steps:
      - run: echo "About to start deploy"
      - deploy:
          name: Deploy Master Branch to AWS
          command: |
            if [ "${CIRCLE_BRANCH}" == "master" ]; then
              pwd
              ls
              ./deploy.sh
            fi
